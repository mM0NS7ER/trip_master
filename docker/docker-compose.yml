version: '3.8'

services:
  trip-master:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: trip-master:latest
    container_name: trip-master-app
    ports:
      - "80:80"
      - "8000:8000"
    volumes:
      - ./backend/.env:/app/.env:ro
    restart: unless-stopped